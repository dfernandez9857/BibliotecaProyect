@model WebApp.Models.Prestamo

@{
    ViewData["Title"] = "Create";


    // var libros = ViewBag.IdLibros;
}

<h1>Registrar</h1>

<h4>Prestamo</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdUsuarioSolicitaPrestamo" class="control-label"></label>
                <select asp-for="IdUsuarioSolicitaPrestamo" class ="form-control" asp-items="ViewBag.IdUsuarioSolicitaPrestamo"></select>
            </div>
            <div class="form-group">
                <label asp-for="Observacion" class="control-label"></label>
                <input asp-for="Observacion" class="form-control" />
                <span asp-validation-for="Observacion" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FechaTentativaDevolver" class="control-label"></label>
                <input asp-for="FechaTentativaDevolver" class="form-control" />
                <span asp-validation-for="FechaTentativaDevolver" class="text-danger"></span>
            </div>
            
            
            <div class="row">
                <div class="col-md-12">
                    <div class="form-group">

                        @* @foreach (var item in libros) {

                        } *@

                        <label asp-for="DetallePrestamos" class="control-label">Libro</label>
                        <select asp-for="DetallePrestamos" class="form-control" asp-items="ViewBag.IdLibros"></select>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>




        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
